#include<stdio.h>
int main() {
  int size;
  printf("Enter the no of elements : ");
  scanf("%d", &size);
  int array[size];
  for(int i = 0; i < size; i++) {
    scanf("%d", &array[i]);
  }
  for(int i = size; i>=0; i--) {
    printf("%d ", array[i]);
  }
  return 0;
}
